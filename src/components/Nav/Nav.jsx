import React from 'react';
import styles from '../stylesheets/modules/components/Nav.module.scss';

const Nav = props => {
  return(
    <header>
      <img src="../../assets/images/logo_circle.png" />
    </header>
  )
};

export default Nav;